a = recipe({
    path: 'a',
    make: a => shell`${a}`
})

b = recipe({
    path: 'b',
    make: b => shell`${b}`
})

c = recipe({
    path: 'c',
    make: c => shell`${c}`
})

d = recipe({
    path: 'd',
    make: d => shell`${c} ${d}`
})
e = recipe({
    path: 'a',
    make: a => shell`${a} ${d} ${e}`
})
