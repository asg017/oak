a = recipe({
    path: 'a',
    make: a => bash`${a}`
})

b = recipe({
    path: 'b',
    make: b => bash`${b}`
})

c = recipe({
    path: 'c',
    make: c => bash`${c}`
})

d = recipe({
    path: 'd',
    make: d => bash`${c} ${d}`
})
e = recipe({
    path: 'a',
    make: a => bash`${a} ${d} ${e}`
})
