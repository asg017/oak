import {g as importedG} from 'Oakfile.import'

h = recipe({
    path: 'h', 
    make: h => shell`${h}`
})

i = recipe({
    path: 'i',
    make: i => shell`${g} ${h} ${i}`
})

j = recipe({
    path: j,
    make: j => shell`${j}`
})