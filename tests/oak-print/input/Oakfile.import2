import {g as importedG} from 'Oakfile.import'

h = recipe({
    path: 'h', 
    make: h => bash`${h}`
})

i = recipe({
    path: 'i',
    make: i => bash`${g} ${h} ${i}`
})

j = recipe({
    path: j,
    make: j => bash`${j}`
})