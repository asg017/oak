echo = async (path, content) => command("echo", ["-n", content], path);

cat = async (path, files) => command("cat", files, path);

a = task({
  path: "a",
  run: a => echo(a, "a")
});

b = task({
  path: "b",
  run: a => echo(a, "b")
});
c = task({
  path: "c",
  run: c => cat(c, [a, b])
});
