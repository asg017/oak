a = recipe({
    path: 'a',
    run: a => shell`${a}`
})

b = recipe({
    path: 'b',
    run: b => shell`${b}`
})

c = recipe({
    path: 'c',
    run: c => shell`${c}`
})

d = recipe({
    path: 'd',
    run: d => shell`${c} ${d}`
})
e = recipe({
    path: 'a',
    run: a => shell`${a} ${d} ${e}`
})
