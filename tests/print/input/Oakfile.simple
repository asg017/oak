a = recipe({
    target: 'a',
    run: a => shell`${a}`
})

b = recipe({
    target: 'b',
    run: b => shell`${b}`
})

c = recipe({
    target: 'c',
    run: c => shell`${c}`
})

d = recipe({
    target: 'd',
    run: d => shell`${c} ${d}`
})
e = recipe({
    target: 'a',
    run: a => shell`${a} ${d} ${e}`
})
