import {g as importedG} from 'Oakfile.import'

h = recipe({
    target: 'h', 
    run: h => shell`${h}`
})

i = recipe({
    target: 'i',
    run: i => shell`${g} ${h} ${i}`
})

j = recipe({
    target: j,
    run: j => shell`${j}`
})