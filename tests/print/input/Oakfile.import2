import {g as importedG} from 'Oakfile.import'

h = recipe({
    path: 'h', 
    run: h => shell`${h}`
})

i = recipe({
    path: 'i',
    run: i => shell`${g} ${h} ${i}`
})

j = recipe({
    path: j,
    run: j => shell`${j}`
})