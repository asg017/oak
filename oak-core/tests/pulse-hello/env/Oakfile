a = task({
  target: "a",
  run: a => shell`echo -n "a" > ${a}`
});
b = task({
  target: "b",
  run: b => shell`echo -n "b" > ${b}`
});
c = task({
  target: "c",
  run: c => shell`cat ${a} ${b} > ${c}`
});
