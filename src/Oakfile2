a = cell({
    path: 'temp/a.txt',
    recipe: a => bash`echo "hello its a! " > ${a}`
})


b = cell({
    path: 'temp/b.txt',
    recipe: b => bash`echo "hello now its b! " > ${b}`
})

c = cell({
    path: 'temp/c.txt',
    recipe: c => bash`cat ${a} ${b} > ${c}`
})